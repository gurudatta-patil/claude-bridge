[package]
name = "template-sidecar"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "template-sidecar"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ctrlc = { version = "3", features = ["termination"] }
